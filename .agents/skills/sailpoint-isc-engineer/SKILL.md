---
name: sailpoint-isc-engineer
description: >-
  Expert assistant for engineers implementing SailPoint Identity Security Cloud
  (ISC/IdentityNow). Use whenever the work touches it: writing or debugging
  transforms (Seaspray JSON), cloud or connector rules (BeanShell),
  Joiner/Mover/Leaver (JML) flows, ISC Workflows (triggers, steps, HTTP
  actions), identity profiles, lifecycle states, sources, aggregation,
  correlation, provisioning, attribute sync, access profiles, roles, segments,
  certifications, SoD policies, the REST API, CLI (sail), SDKs, or
  sandbox-to-production promotion (spconfig). Also for apps/portals/BFFs
  calling ISC: OAuth/PKCE/PAT credential choice per call, executing external
  workflows, deciding approvals, or role-membership payloads via API. Trigger
  even when the user just names an object ("write a firstValid transform",
  "this aggregation isn't correlating") without saying "SailPoint".
version: 1.6.0
---

# sailpoint-isc-engineer — Proxy Entry

This entry exists for IDE auto-discovery. The full skill content is at the canonical plugin path:

```
C:\Projetos\iga-isc-skills\plugins\sailpoint-isc-engineer\SKILL.md
```

**When activating this skill, read the full content from that path before proceeding.**

## Quick reference (domain map)

| Task | Reference file inside plugin |
|------|------------------------------|
| Transforms (Seaspray JSON) | `references/transforms.md` |
| Cloud rules & connector rules | `references/rules.md` |
| Workflows | `references/workflows.md` |
| Identity profiles / lifecycle states | `references/identity-profiles-lifecycle.md` |
| Sources / connectors / aggregation / provisioning | `references/sources-provisioning.md` |
| Governance (entitlements / roles / certifications / SoD) | `references/governance.md` |
| Password policies | `references/password-management.md` |
| ISC REST API | `references/api.md` |
| App integration (PKCE, PAT, role-membership payloads) | `references/app-integration.md` |
| SailPoint CLI (`sail`) / SDKs / spconfig promotion | `references/cli-sdk.md` |

All reference files are under:
```
C:\Projetos\iga-isc-skills\plugins\sailpoint-isc-engineer\references\
```

## Current version
`sailpoint-isc-engineer` **1.6.0** (released 2026-07-13).  
Check `C:\Projetos\iga-isc-skills\downloads\VERSIONS.md` to confirm this is current.

